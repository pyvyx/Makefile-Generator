SOURCE_FILES=test.cpp Dependencies/GLFW/er.cpp
OBJ_FILES=$(addprefix out/, $(notdir $(SOURCE_FILES:.cpp=.o)))

Build: $(OBJ_FILES)
	$(info $(SOURCE_FILES))
	$(info $(OBJ_FILES))

out/%.o: %.cpp
	$(info imHere)
	g++ -c $< -o $@

out/%.o: Dependencies/GLFW/%.cpp
	$(info imHere)
	g++ -c $< -o $@